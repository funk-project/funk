; funk/std — strings

package "funk/std/strings" {
  version 0.0.1
}

fn concat {
  name "Concatenate"
  doc """
    # Concatenate
    ### Concatenate two strings into one.
  """
  in  (a Str (doc "The left string"))
      (b Str (doc "The right string, appended after `a`"))
  out (r Str (doc "The joined string `a` followed by `b`"))
  examples """
    - `(concat "foo" "bar")` → `"foobar"`
    - `(concat "a" "")` → `"a"`
  """
  engine builtin
  src """
    str.concat
  """
}

fn upper {
  name "Uppercase"
  doc """
    # Uppercase
    ### Convert all characters in a string to uppercase.
  """
  in  (a Str (doc "The string to convert"))
  out (r Str (doc "The uppercased string"))
  examples """
    - `(upper "hello")` → `"HELLO"`
    - `(upper "aB")` → `"AB"`
  """
  engine builtin
  src """
    str.upper
  """
}

fn lower {
  name "Lowercase"
  doc """
    # Lowercase
    ### Convert all characters in a string to lowercase.
  """
  in  (a Str (doc "The string to convert"))
  out (r Str (doc "The lowercased string"))
  examples """
    - `(lower "HELLO")` → `"hello"`
    - `(lower "aB")` → `"ab"`
  """
  engine builtin
  src """
    str.lower
  """
}

fn trim {
  name "Trim"
  doc """
    # Trim
    ### Remove leading and trailing whitespace from a string.
  """
  in  (a Str (doc "The string to trim"))
  out (r Str (doc "The string with surrounding whitespace removed"))
  examples """
    - `(trim "  hi  ")` → `"hi"`
    - `(trim "x ")` → `"x"`
  """
  engine builtin
  src """
    str.trim
  """
}

fn length {
  name "Length"
  doc """
    # Length
    ### The number of characters in a string.
  """
  in  (a Str (doc "The string to measure"))
  out (r Num (doc "The character count of `a`"))
  examples """
    - `(length "hello")` → `5`
    - `(length "")` → `0`
  """
  engine builtin
  src """
    str.len
  """
}
